// The seed, in one place. An arbitrary hex here; the real brand's hex replaces it where
// the proof is shown. The secondary is derived by the engine, and the lane is WCAG.
export const SEED = '#E93D82'
export const BRAND = 'poc'
export const PROFILE = 'wcag' as const
